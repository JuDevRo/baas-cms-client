Instrucciones frontend

Seguimos trabajando en el componente de Projects.

Ahora vamos a trabajar la parte de entries que es donde el usuario puede subir valores segun los campos determinados que ya creo que vienen en data.structure y ahora necesitamos que toda la informacion que suba se guarde en la propiedad .data del scheme

1- Necesitamos visualizar como hicimos con structure pero con data en la parte de entries. Que sea una tabla responsive con los campos de structure y debajo los valores subidos por el usuario, si el usuario no subio nada solo los titulos de las columnas.

2- Crear el modal para subir los valores segun los campos creados, es muy importante gestionar las imagenes, si es una o varias que funcione bien. Necesito que el usuario pueda directamente subir sus archivos y luego yo desde el backend los voy a procesar y subir a un bucket.

3- Tiene que revisar si hay campos o no requeridos antes de dejar crear, piensa tu tambien que tiene sentido agregar como validacion antes de crear dentro de la logica.

4- La ruta para guardar es la misma que usamos para structure pero ahora se llama data.

5- Una vez hay informacion en entries subida por el usuario un boton para borrar y otro para editar serian lo conveniente. Probablemente sea necesario el modal para editar.


Usa el index.css para entender la logica de estilos.

Todo tiene que ser responsive

Si tienes dudas consultame antes de ejecutar.